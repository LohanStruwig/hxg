{
  "name": "hxg-evidence-explorer",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "sync-data": "node scripts/sync-data.mjs",
    "dev": "npm run sync-data && astro dev",
    "build": "npm run sync-data && astro build",
    "check": "astro check"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.6",
    "astro": "^7.2.4",
    "cytoscape": "^3.33.1",
    "sharp": "^0.35.3",
    "typescript": "^5.9.2"
  },
  "devDependencies": {
    "@types/node": "^24.3.0"
  }
}
